# Cake

### RO:

Enunțul ne spune de stringul `applepie`. Ne uităm la cookies, observăm un cookie `flag`, și în loc de hidden, punem `applepie`. Dând refresh la pagină, primim flagul.

### EN:

The statement mentions the string `applepie` and by looking at the site's cookies, we see the `flag` cookie. By replacing `hidden` with `applepie` and refreshing the page, we get the flag.
