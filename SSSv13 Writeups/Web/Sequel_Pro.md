# Sequel Pro

### RO:

Numele challului ne face să ne gândim la SQL Injection, astfel folosind perechea de credențiale `"admin":"' OR '1' = '1"`. Prin login, obținem flagul.

### EN

The name of the chall makes us think of SQL Injection, and so, we can use the credential pair `"admin":"' OR '1' = '1"`. By pressing the login button, we get the flag.
