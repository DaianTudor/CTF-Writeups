# In your face

### RO:

Folosind Inspect Element, observăm un comentariu HTML encodat în baza 64. Decodând, obținem flagul.

### EN:

Using Inspect Element, we see an HTML comment encoded in base 64. By decoding, we get the flag.
